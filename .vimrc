" Set advanced features in Vim
set nocp

" Enable mouse
set mouse=a

" Turn on line numbering
set number

" Activate syntax highlighting
syntax enable

" Set solarized background mode (dark or light)
set background=dark
colorscheme solarized

" Indentation with 2 spaces instead of tabs
set expandtab
set shiftwidth=2
set softtabstop=2

" New line automatically copies previous line's indentation
set autoindent
